[pytest]
# Only look for tests in tests/ — avoids picking up src/backend/test_*.py
# (those are not pytest-style and would fail collection anyway).
testpaths = tests

# Add the project root (where main.py lives) to sys.path so
# `from main import app` works from inside tests/.
pythonpath = .