# Recommender service dependencies. The real LLM backends are optional
# and only required if you set AI_BACKEND=openai or AI_BACKEND=lmstudio.
#
# Stub backend (default, no LLM needed):
#   pip install -r requirements.txt
#
# For OpenAI or LM Studio backends, also install:
#   pip install openai
# (already included below — it's tiny and means no surprise at runtime.)
fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
openai>=2.43,<3

# Test-only. Move to requirements-dev.txt when you split things up.
pytest==9.1.1