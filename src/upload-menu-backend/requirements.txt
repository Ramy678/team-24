# Upload-menu service dependencies.
# Pinned to the versions installed in our shared venv.
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pytesseract==0.3.13
Pillow==11.0.0

# Test-only. Move to requirements-dev.txt if you split things up later.
pytest==9.1.1